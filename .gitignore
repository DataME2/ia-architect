node_modules/
.next/
out/
# Generated by `next build`; it references files under .next/ that are not
# committed, so it is regenerated rather than tracked.
next-env.d.ts

# Local Claude Code agent worktree scratch space — not project content.
.claude/worktrees/

# Secrets. Never commit any of these — this repository is public, and a
# service-role key here bypasses every RLS policy for every club at once.
.env
.env.*
!.env.example
*.pem
*.key

*.tsbuildinfo
.DS_Store
