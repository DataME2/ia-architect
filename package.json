{
  "name": "letsdatatalk",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Multiclub, multitenant football club operations platform (AU/NZ)",
  "engines": {
    "node": ">=22.6"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test 'src/**/*.test.ts'",
    "check": "npm run typecheck && npm run test && python3 scripts/check_links.py && python3 scripts/check_rls.py"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3"
  }
}
