# Application Components

_[← Application layer](./README.md) · [EA home](../README.md)_

**ArchiMate elements:** Application Component, Application Interface.

The components providing the
[application services](./1_application-services.md), each mapped to where
it lives in the source tree.

> **Grounding rule.** Every row points at the module that implements it.
> Rows marked *planned* have no code yet — they are the build list for
> [scope document 17](../../scope/17_mvp-registration-slice.md), and each
> becomes a real path as it lands.

**This document lists what exists.** For what the software *offers* and how
much of each service is real — including the three that are partial and the
eight capabilities with no code at all — see
[1_application-services.md](./1_application-services.md). Reading this page
alone gives a misleading impression, because a list of delivered components
says nothing about the ones that were never started.

## Components

| Component | Source | Provides | Status |
| --------- | ------ | -------- | ------ |
| **Domain model** | `src/domain/types.ts` | `Person`, `LegalName`, `Guardianship`, `Consent`, `Registration`, and the age/minority helpers | **Delivered** |
| **Rules engine** | `src/domain/rules/` | BR1, BR2, BR3, BR48, BR55 as individually testable pure functions, plus the registry and evaluator in `index.ts` | **Delivered** |
| **Duplicate detection** | `src/domain/identity/br5-duplicate-candidates.ts` | BR5 — candidates for human confirmation, never an automatic merge | **Delivered** |
| **Submission pack builder** | `src/domain/submission/` | Assembles, versions, freezes and serialises a pack (BR58, BR59); keeps *sent* and *registered* apart (BR60) in `status.ts`; CSV output in `serialise.ts` | **Delivered** |
| **Schema & RLS policies** | `supabase/migrations/0001_registration_slice.sql`, `0002_rls_policies.sql` | 13 tables, RLS enabled in the same migration that creates them, policies in the next | **Delivered** |
| **Policy coverage gate** | `scripts/check_rls.py` | Fails the build if any table lacks RLS, a policy, or a `club_id` — static analysis of the migration text | **Delivered** |
| **Tenant isolation test** | `scripts/test_rls.sh`, `supabase/tests/10_tenant_isolation.sql` | Applies the real migrations to a throwaway Postgres and asserts isolation **behaviourally** across 11 scenarios, as the `authenticated` role — owners bypass RLS, so a test run as `postgres` would pass regardless of how broken the policies were. Verified to fail on a loosened policy | **Delivered** |
| **Pack immutability test** | `supabase/tests/11_submission_pack.sql` | Nine scenarios proving BR58 holds in the database, not just in application code: a handover can be recorded once and never rewritten, a pack cannot be deleted, the manifest round-trips, an undefined submission state is refused, and another club sees none of it. Verified to fail when the handover policy is loosened | **Delivered** |
| **Configuration & clients** | `src/data/env.ts`, `src/data/client.ts` | Validated config, and three Supabase clients that differ in one way that matters — whether RLS applies. `createAdminClient(reason)` bypasses it, throws in a browser, and takes a reason from a closed set so every bypass is greppable | **Delivered** |
| **Registrar screens** | `src/app/registrar/` | The season queue grouped by what it is waiting on, a per-registration detail with every rule's outcome, BR55 legal-name verification, and BR5 duplicate candidates surfaced for a human | **Delivered** |
| **Family registration flow** | `src/app/register/` | The collect-once form: legal and preferred names kept apart, guardian required for a minor, the three consents recorded independently | **Delivered** |
| **Public registration link** | `src/app/join/[token]/`, `src/data/invitations.ts`, `src/web/invitation-view.ts` | A family registers with no account (BR72). The page shows nothing about the club or season — rendering either would turn a write-only link into a read of tenant data — and the token is checked by the database on submit | **Delivered** |
| **Invitation management** | `src/app/registrar/invitations/` | Issue, list and revoke links per season. The link is displayed **once** at issue and only its hash is stored, so it cannot be shown again (BR73) | **Delivered** |
| **Public write surface** | `supabase/migrations/0005_registration_invitations.sql` | One `security definer` function, `submit_public_registration`, with a pinned `search_path`. The tenant is **not an argument** — it comes from the invitation the token resolves to — and BR1/BR48 are enforced here because an anonymous caller can skip the form. See [decision 6](../../decisions/6_public-registration-through-a-scoped-function.md) | **Delivered** |
| **Public registration test** | `supabase/tests/12_public_registration.sql` | Eighteen scenarios as `anon`: a valid link writes only to its own club, an unknown/revoked/expired token is refused identically, the link reads nothing and cannot insert outside the function, and the token hash matches the one TypeScript computes. Verified to fail on a weakened function | **Delivered** |
| **Submission pack screens** | `src/app/registrar/pack/`, `src/data/packs.ts` | Preview of exactly what would be sent and who would be left out, versioned generation (BR58), handover recorded once against a named channel (BR59), CSV download served from the stored manifest, and per-person outcomes — the only route to `COMPLETE` (BR60) | **Delivered** |
| **People directory** | `src/app/registrar/people/`, `src/web/people-view.ts` | One row per human, with the roles they hold this season and the people they are responsible for in both directions — and grant/revoke for each role. **The first surface where P1 is visible**: a parent who also coaches is one record with two roles, and a guardian created by a family's submission is findable rather than existing only as the far end of a foreign key | **Delivered** |
| **Season requirements** | `src/app/registrar/season/`, `src/web/money.ts` | The club's per-season document checklist and registration fee. Copied onto every registration as it is created, which is what gives BR2 and BR3 something to check — before this they compared an empty list against an empty list and passed on everything | **Delivered** |
| **Requirements & roles at intake** | `supabase/migrations/0006_season_requirements_and_roles.sql` | Replaces `submit_public_registration` so a public submission stamps the checklist, the fee, and the `player` and `guardian` roles. The fix is at the point registrations are created rather than in each screen that reads one; security properties unchanged — still `security definer`, still a pinned `search_path`, still no club or season argument | **Delivered** |
| **Requirements & roles test** | `supabase/tests/13_requirements_and_roles.sql` | Fifteen scenarios: the checklist and fee land on a public submission and none arrives pre-satisfied, both roles are written, and a registrar still cannot grant a role at another club or stamp one with another club's id. Verified to fail — with the checklist loop and player role removed it reports *found 0 — BR2 would pass vacuously* | **Delivered** |
| **Payment plans** | `src/domain/finance/`, `src/data/finance.ts`, `src/web/plan-view.ts` | Cent-exact schedule generation (BR74), the season boundary (BR76), and oldest-first allocation computed at read time rather than stored — a family paying $50 against a $40 instalment has made no statement about allocation, and storing a guess turns arithmetic into a disputed fact | **Delivered** |
| **Payment plan screens** | `src/app/registrar/[registrationId]/PaymentPlanPanel.tsx` | Agree a plan with a live schedule preview, the instalment ledger with what is paid and what is overdue, and receipt recording. Only an admin or treasurer sees the controls (BR78) — and the database is what enforces it, so this is a hidden button rather than the control | **Delivered** |
| **Payment plan test** | `supabase/tests/14_payment_plans.sql` | Fifteen scenarios: a plan short of its total is refused at commit and so is raising the total without rescheduling, a receipt cannot be rewritten or deleted, a registrar may read but not record, and another club's treasurer sees none of it. Verified to fail — dropping the trigger or loosening the receipt policy produces exactly the assertions above | **Delivered** |
| **Demo club** | `supabase/demo/seed.sql`, `teardown.sql` | A permanently-seeded club of obviously fictional data, for showing a prospect what the product does rather than an empty shell. **A real tenant** — P5 protects it like any other, and BR83 was verified still refusing an uncleared coach inside it. Registrations are created by calling `app_create_registration`, the same function both real surfaces use, so the demo cannot drift from real behaviour. **Deliberately not a migration**: `supabase/migrations/` runs against production on merge, and a demo club appearing in the pilot club's project because a file moved is the failure the header warning exists to prevent | **Delivered** |
| **Form outcomes** | `src/web/form-result.ts`, `_components/FormNotice.tsx` | One shape for what a form action hands back, rendering **success as well as failure**. Every action previously returned `string | null` — a message when something went wrong and **nothing at all when it worked** — so a registrar who recorded a Working with Children Check got no acknowledgement of any kind. Silence is not a success state: it is indistinguishable from a click that did not register, and it teaches people to press twice | **Delivered** |
| **Club governance** | `src/domain/governance/`, `src/data/governance.ts`, `src/app/registrar/governance/` | Committee terms, positions, and whether the mandate has lapsed. Reading is open to every club member — a registrar chasing BR21 needs to know who the Committee is — while changing it is an admin act | **Delivered** |
| **Match official clearance** | `supabase/migrations/0011_match_officials_and_governance.sql` | BR84 as a trigger on `person_role` for referee and coach, with the **under-18 exemption** that 0010 was missing. `app_needs_clearance` answers it by age at season end; `supabase/tests/18` asserts the exemption as hard as the requirement, because an over-strict safeguarding rule fails quietly | **Delivered** |
| **Teams &amp; officials** | `src/domain/teams/`, `src/data/teams.ts`, `src/app/registrar/teams/` | Players by team, officials with their clearance state, and the forms to build both. `mayHoldRole` answers BR19/BR54 in the screen so a refusal is a sentence rather than a 500; the database refuses independently | **Delivered** |
| **No card, no start** | `supabase/migrations/0010_teams_and_clearances.sql` | BR83 as a trigger on `team_member`, firing on insert **and update** — promoting a player row to a coach row is the obvious way round an insert-only check. Measured against the season's `ends_on`, never against today. `supabase/tests/17` proves it, and was verified to fail when the check is weakened to *valid today* or narrowed to insert-only | **Delivered** |
| **Reissue a registration link** | `src/app/registrar/invitations/ReissueButton.tsx` | There is no "show the current link" button and there cannot be — only the token's hash is stored (BR73). Reissue revokes the old link and displays a replacement once, which is the honest answer to a family who lost theirs | **Delivered** |
| **One creation path** | `supabase/migrations/0009_one_creation_path_and_merging.sql` | `app_create_registration` holds the player registration process once; `submit_public_registration` (definer, token) and `submit_club_registration` (invoker, RLS) are thin wrappers. Written after the club's own screen was found building registrations out of six inserts and falling behind every rule added since — no roles, no checklist, no fee, a duplicated parent each time. `supabase/tests/16` asserts the two surfaces produce the *same* registration, which is the check that would have caught it | **Delivered** |
| **Duplicate resolution** | `src/app/registrar/duplicates/`, `merge_person()` | Club-wide duplicate detection and a human-chosen survivor (BR5, BR82). Club-wide because the per-registration view structurally cannot see the worst case: a guardian has no registration, so four copies of one parent appeared on no screen at all | **Delivered** |
| **Vouchers** | `src/domain/finance/voucher.ts`, `src/data/vouchers.ts`, `src/app/registrar/[registrationId]/VoucherPanel.tsx` | Attach a voucher with its PDF, and a human decides. Attaching moves no money (BR81); verifying records a payment of method `voucher`, so relief shares the ledger with every other receipt and a rejection reverses it under BR77. A registrar may attach — collecting the document is registration work — but only an admin or treasurer may verify, because that is the act that moves money (BR78) | **Delivered** |
| **Voucher file storage** | `supabase/migrations/0008_vouchers_and_shared_guardians.sql` | A private Supabase Storage bucket whose object paths begin with the `club_id`, and policies that check exactly that segment — so a member of one club cannot read or write another's folder even knowing the path. **Not covered by the behavioural SQL suite**: the `storage` schema does not exist on the local test Postgres, so the bucket and its policies are created behind a guard and go unproved by CI | **Delivered, unproved** |
| **Play eligibility (BR79)** | `src/domain/finance/eligibility.ts` | No pay, no play, asked fresh from the registration status **and** the balance together, every time — never stored. Catches the case neither input can catch alone: a Player confirmed by the federation and charged later keeps a frozen `COMPLETE` status (BR60) while owing money, so every screen asking "is this registration finished?" answers yes. The queue surfaces them as *Registered, but cannot play* | **Delivered** |
| **Derived registration status** | `src/domain/rules/registration-status.ts` | Moves a registration to `PENDING_DOCUMENTS` or `PENDING_PAYMENT` from its rule outcomes. Until this existed nothing in the code moved the status at all — a registration sat at `DRAFT` until a pack carried it away, and the two statuses that exist to say *why* it was waiting were never once written. It refuses to produce `COMPLETE` or `PENDING_EXTERNAL_REGISTRATION`, which stay the federation's and the pack's (BR43, BR60) | **Delivered** |
| **Interface design system** | `src/app/globals.css` | The tokens every screen is composed from — colour ramps, semantic aliases, space, radius, elevation, type and motion — with a dark theme that redefines only the tokens rather than restating the rules. **The palette is an argument, not a preference**: green and gold is what football looks like in Australia, and it sits under New Zealand's white without competing with it, so one palette serves both markets. Two constraints keep it usable — gold never carries text (roughly 1.6:1 on white, so it is the rail, the ring and the focus halo and never a word), and **no state is signalled by hue alone**: every status pill carries a dot and a word, because the thing a pill marks is whether a child may take the field. It replaced a stylesheet grown page by page, in which four classes the markup used (`prose`, `mono`, `table-wrap`, `process-detail`) had **no rule at all** — five components had been rendering unstyled. Still no framework, no component library and no web font: `next/font` fetches at build time, and a typeface is not worth a build that fails because a CDN is unreachable ([scope 31](../../scope/31_the-interface-and-the-southern-ocean-palette.md)) | **Delivered** |
| **Application shell and front door** | `src/app/layout.tsx`, `src/app/page.tsx`, `src/app/_components/BrandMark.tsx` | A sticky masthead carrying the mark — an inline SVG football panel in green, gold and white — a keyboard skip link past the ten-destination club menu, a per-scheme `viewport` theme colour so a phone's browser chrome continues the page, and a footer. The landing page separates its two audiences before either reads a menu: a family with a child to register, and an officer starting a shift. **Deliberately still makes no auth call** — it is prerendered, which is why there is no *Sign in* link in the masthead: it would be offered to people already signed in, and `/registrar` already sends whoever needs it to sign-in with their destination attached | **Delivered** |
| **Role context (BR61)** | `src/web/role-context.ts` | Which role a Person is acting in, and what that context may show — **the first execution of a rule documented since the business layer was drafted and never once run**. Pure, no I/O, no DOM, under the `tsconfig.domain.json` guard, so every claim about what a guardian may see is a unit test rather than a screen somebody eyeballed. Two things it does deliberately: the active context is **derived, never stored** (it is a lens over `club_membership` and `person_role`, and persisting it would create a second source of truth), and `resolveActive` **refuses to guess** — several roles held and none chosen returns *ask*, never the first role, the most recent, or the busiest, because each of those is a switch the Person did not make. Also holds **BR6 and BR109**: `appointmentConflict` answers both, and `offerableAppointments` removes the conflicted fixture entirely rather than showing a refusal ([scope 32](../../scope/32_the-role-context-shell-and-a-second-palette.md)) | **Delivered** |
| **Identity rail and role switcher** | `src/app/_components/IdentityRail.tsx` | The Person, their clubs and the roles they hold — identical in every context, so only the workspace redraws. The switcher is **links, not client state**: a role context is a URL, which makes it shareable, back-buttonable, and keeps BR61's *explicit switching* true through the browser's history buttons. The ledger counts **one sign-in**, not one Person: a Person is tenant-scoped and cross-club identity is BR44's problem, so across clubs the constant thing is the credential, and the ledger says so rather than overclaiming. Prints BR61 where the person it governs can read it. Holds the **crest slot**, dashed and labelled until a licensed asset exists ([#66](../../scope/open-questions.md)) | **Delivered** |
| **The person-facing shell (`/me`)** | `src/app/me/page.tsx`, `src/data/me.ts`, `src/web/me-view.ts` | One record, operated through five lenses. `loadMe` **derives** the role list from `account_person`, `person_role`, `guardianship`, `team_member` and `club_membership` — it stores nothing — and `resolveActive` decides which lens is open: several roles held and none chosen renders **the question**, not a default (BR61). Three honest empty states, not one: no club membership, an account nobody has linked to a person (BR108), and a linked person with no role this season. The decisions — next fixture, season figures, the guardian's scope, initials — are pure in `src/web/me-view.ts` and unit-tested without a DOM | **Delivered** |
| **Five role workspaces** | `src/app/me/_workspaces/` | Player, coach, referee, guardian, committee. Each renders what the schema can answer today and marks the rest **coming soon, named for what it waits on** — a rule, a capability, a table — so a reader can tell a week from a season. Guardian and committee are mostly live (registration, consents, fees, the governing term, vacant offices, BR78 absent-not-refused). Player and coach carry real fixtures, rosters and BR79's *cannot be selected*, and wait on BR62 for availability. **The referee workspace is honest about RLS**: every referee table is readable only by admin, registrar and coordinator (0023–0025), so an official signed in as themselves sees *coming soon — waits on a read policy*, never an empty list dressed as "none" (BR65). Nothing here writes | **Delivered, with named gaps** |
| **Sign-in, in two halves** | `src/app/sign-in/page.tsx` | The reef panel says what one sign-in gets you — every role, one record, no second account — before anything is typed, because the person most likely here is a parent with a coaching login about to make a second one. The form is unchanged in what it does; `/me` is added to the destination allowlist so a sign-in that started from *your roles* lands there | **Delivered** |
| **The Assistant's surface** | `src/app/_components/AssistantNote.tsx` | Renders a draft and exactly two controls — use it, or dismiss it. **There is no prop, variant or escape hatch that commits anything**, and that is the point: [decision 1](../../decisions/1_ai-assistant-autonomy-level.md) expressed as a type rather than as a promise. A component that renders a draft invites the next change to make it *produce* one and the change after that to add a send button "just for reminders"; one that structurally cannot render a committing control can only acquire one by a deliberate edit to that file, which is where the argument belongs. **No inference — nothing here calls a model** | **Delivered** |
| **View logic** | `src/web/` | The screens' decisions as pure, framework-free functions — form parsing, queue grouping, the blocker summary, timezone-correct "today", and the sign-in destination allowlist | **Delivered** |
| **First sign-in password, and the way back** | `supabase/migrations/0019_first_sign_in_password.sql`, `src/app/set-password/`, `src/app/sign-in/ResetForm.tsx`, `src/web/password.ts`, `src/proxy.ts` | Somebody arriving on an invitation link is made to choose a password before going further (BR98), and anybody who forgets one can ask for a link themselves. **No password is ever emailed.** `auth.users.encrypted_password` cannot answer "have they chosen one" — Supabase populates it for magic-link users too — so the fact is recorded by the application in a table no client can read or write. Rules are length-first: composition requirements reliably produce `Password1!`. The reset form **answers identically whether or not the address exists**, because an unauthenticated form that distinguishes them is a directory anybody can query. **Corrected September 2026: the page was closed to the people it is for.** `/set-password` was listed as a path requiring a session, so an anonymous visitor was redirected to `/sign-in` — a form asking for the password they had come to create. Anybody whose invitation had expired was left with no route except asking for another link by hand, which is the support burden this component exists to remove. The path is no longer guarded by the proxy; signed out it asks for an address and sends a link, and the action that actually changes a password still refuses without a session and changes the password of whoever holds it rather than one named in the form. A dead or missing callback link lands here too, for the same reason | **Delivered** |
| **Licensing and the tenant roll** | `supabase/migrations/0018_club_licence.sql`, `src/web/platform-view.ts`, `src/app/platform/LicenceForm.tsx` | How many clubs are supported and which of them are customers. A licence is a **term** — start, end, state, and the fee negotiated with that club — so a renewal is a new row and last year's price is still answerable. **The end date beats the state column**: a licence still marked active whose term ended in June counts as lapsed, because trusting the flag is how the console reports a flattering number. The demonstration club is excluded from every count, and only fees actually agreed are totalled. **Nothing is gated by it** (BR97) — see [#61](../../scope/open-questions.md) | **Delivered** |
| **Identification photograph** | `supabase/migrations/0021_identification_photograph.sql`, `src/web/photo-crop.ts`, `src/app/registrar/players/[registrationId]/PhotoCropper.tsx`, `src/data/photos.ts` | Choose, frame and store a headshot. **BR56 is enforced here for the first time**: `person.photo_path` had existed since the first migration, nothing ever wrote it, and the rule that a photograph is held only under a consent recorded for that purpose was documented and unchecked — so the trigger went in before the uploader. Removal stays possible however the consent stands, because a withdrawn consent must not trap the picture it covered. The crop is hand-drawn on a canvas rather than pulled from a library: the arithmetic is a few dozen lines in `src/web/` and unit-tested without a browser, and **the canvas step is what strips the EXIF** — including the GPS coordinates a phone attaches to a photograph of a child (BR105). A third private bucket, club-scoped like the others; read reaches the roles that identify players, write stays with admin and registrar | **Delivered** |
| **Player record** | `supabase/migrations/0020_player_record.sql`, `src/domain/performance/`, `src/web/player-view.ts`, `src/app/registrar/players/`, `src/app/registrar/fixtures/` | A player's season: photograph, physique, position, appearances, minutes, goals and assists. **It needed a match model first** — twenty-eight tables and not one of them knew a game had happened, so the numbers had no source. Two tables supply it: a fixture, and one row per player per fixture, carrying **counts rather than events** (a goal is an integer, not a row with a minute and a coordinate) — which is exactly where the paid data tier begins. **Physique is narrowed on read** to the roles that pick teams, following `clearance` rather than the schema-wide default, because a height and a weight are measurements of a child (BR99). Every statistic records who entered it, since there is no fixture feed and no referee report to check it against (BR101) | **Delivered** |
| **Club onboarding: contacts and claimed access** | `supabase/migrations/0017_club_contacts_and_claimed_access.sql`, `src/app/platform/`, `src/app/auth/callback/route.ts` | A club is provisioned with the people answerable for it — name, email, phone, and a deputy — and they are emailed a sign-in link **by Supabase, on the anon key**. Their administrator access exists the moment they follow it. The alternative was the service-role key in a server action, which bypasses RLS for every club at once; removing a support step is not worth that. `claim_club_access` reads the email from the caller's **own session and never from an argument** — a version taking one was written and shown to let a stranger seize a club by naming its contact address (BR94, BR95) | **Delivered** |
| **Club access management** | `supabase/migrations/0015_club_access_management.sql`, `src/app/registrar/access/`, `src/web/access-view.ts` | Who may act at this club, changed from inside the application instead of by the platform owner running SQL against production. The *writes* were always permitted — `club_membership_manage` has allowed an admin to manage memberships since 0002 — but `club_membership` stores a bare `user_id` and `auth.users` is not readable through the API, so a screen built on the policy alone would have listed uuids. Three `security definer` functions supply the missing read and nothing more, each **deriving the club from the caller's own admin membership rather than accepting one**. Not a directory: an account with no membership at that club is invisible. **A club cannot remove its last administrator** — refused in the database, explained in the interface rather than offered and denied | **Delivered** |
| **Demonstration front door** | `supabase/migrations/0013_demo_front_door.sql`, `src/app/demo/actions.ts`, `EnterDemoForm.tsx`, `src/web/prospect-form.ts` | An email address, an optional phone number, no password, no account — and the visitor is inside the demonstration club, reading everything and able to change nothing. The reading is done **through an anonymous Supabase session**, so `auth.uid()` is a real subject and every existing policy applies unchanged; the not-changing is done by a **`viewer` membership**, a role that appears in no write policy anywhere and therefore needs none of its own. `enter_demo()` looks the club up rather than accepting it as an argument, so no request reaches a real tenant. The captured lead lands in `prospect`, outside the club-scoped world and readable through no API at all, together with **marketing consent recorded as a moment and the exact words shown** (BR93) — asked for with an unticked box that never gates entry, and stored from a server-side constant so what was agreed to and what was rendered cannot diverge. See [decision 8](../../decisions/8_demo_access_by_anonymous_session_and_a_read_only_role.md) | **Delivered** |
| **"Which club am I in?"** | `src/app/demo/page.tsx` | The same question the session strip answers, asked from the front door — before you open a screen and start reading records without being sure whose they are. Three answers, not two: signed out, signed in with no membership, the demonstration club, or a real club named outright. **Not the demo and not signed in are styled apart on purpose** — mistaking a real club's records for fictional ones is the error that costs something, and it does not get to look like the reassuring one. Its own route rather than a panel on `/`, because answering it needs an auth call and `/` is prerendered deliberately; the landing page only links here | **Delivered** |
| **Club menu, and the demo marker** | `src/app/registrar/_components/RegistrarNav.tsx`, `src/web/nav.ts` | Every club-facing destination, on every club-facing screen, with the current one lit. Before this the menu was a row of buttons on the queue and every other screen offered one link — *back to the queue* — so any journey between two screens passed through a third, and no screen said where it sat. **The chosen season travels with each link**, because a queue for 2027 beside a pack for 2026 is a mismatch nobody notices until the pack goes out. The demonstration club announces itself here too: a banner and a badge, decided by the same `(DEMO)` marker `teardown.sql` refuses to delete without — one convention checked in two places, rather than a column a real club could be given by accident. The decision (which link is current, which carries a season, what counts as the demo) is pure and unit-tested in `src/web/`; the component renders it | **Delivered** |
| **Session visibility** | `src/app/registrar/layout.tsx`, `_components/SessionStrip.tsx` | Who you are signed in as — email, club, **every role held**, and when the session began — plus the way out. The roles are the load-bearing part: what these screens allow turns on them (BR78, BR83), so a registrar who cannot see a verify button needs to tell at a glance that it is their role rather than a broken button. **`signOutAction` already existed and nothing called it** — you could sign in and never sign out, which on a shared club computer means the session stays open for whoever sits down next. Scoped to a nested layout rather than the root, so the landing page and the public join link stay static and make no auth call | **Delivered** |
| **Session & routing** | `src/app/sign-in/`, `src/proxy.ts` | Email/password sign-in, and the request proxy that refreshes the Supabase session so `auth.uid()` keeps resolving | **Delivered** |
| **Typed queries** | `src/data/queries.ts`, `src/data/schema.ts`, `src/data/mappers.ts`, `src/data/server.ts` | Request-scoped RLS-respecting clients, hand-written row types, and pure row→domain mappers | **Delivered** |
| **The referee's own record** | `supabase/migrations/0023_referee_record.sql`, `src/data/officiating.ts`, `src/app/registrar/referees/`, `src/web/referee-view.ts` | C4's first code. A `referee_profile` on a Person who is already one (P1), with **classification as dated history rather than a current column** (BR110), accreditations checked against a fixture's date (BR111), and suspensions. The roster screen shows who may officiate what, and why not | **Delivered** |
| **Declaring when an official can officiate** | `supabase/migrations/0024_referee_availability.sql`, `src/web/availability-view.ts`, `src/app/registrar/referees/AvailabilityPanel.tsx` | Season availability and explicit unavailability, declared by the official (BR62). Two tables rather than one, because "has not said" and "has said no" are different answers and only the second is a fact | **Delivered** |
| **Designating an official, and the conflicts that refuse it** | `supabase/migrations/0025_match_official_appointment.sql`, `src/domain/officiating/conflicts.ts`, `src/app/registrar/designations/` | The blocking checks (BR6–BR10) and BR109's wider reading — the same Person holding *any* other role in that fixture, coach or guardian included, not only a registered player. BR11's judgement calls warn and are audited instead. A decline or withdrawal **is not recorded at all until a reason is given** (BR42, BR112), and every designation records the appointing party (BR114) | **Delivered** |
| **Verification, and the rates a club sets** | `supabase/migrations/0026_verification_and_fee_schedule.sql`, `src/domain/officiating/fees.ts` | BR13's precondition: a claim needs a verified match, and **nobody verifies the match they were paid for** (BR119) — the separation BR78 draws for player money, applied where it is the same person's own claim. A fee schedule is a **dated version, not an edited row** (BR115) | **Delivered** |
| **Claims, approval and batches** | `supabase/migrations/0027_referee_claims_and_batches.sql`, `src/data/officiating.ts` | A claim carries **the amount it was computed at** (BR116), not a rate resolved on render. No claim for a cancelled match, an explanation required for an abandoned one (BR17, BR18), no double payment (BR14), and a batch **closed before it is paid** (BR117). A remittance records what the club paid through its own bank — the platform never initiates a transfer (BR118) | **Delivered** |
| **Referee slice tests** | `supabase/tests/26_referee_record.sql` … `30_referee_claims_and_batches.sql` | Five scenario files carrying the referee half of the product, on the same terms as the rest: a policy that is present and wrong fails here rather than silently | **Delivered** |
| **Inviting a guardian to their own workspace** | `supabase/migrations/0028_family_access.sql`, `src/data/family.ts`, `src/app/registrar/[registrationId]/GuardianInvite.tsx`, `src/app/auth/callback/route.ts` | Scope 35's WP4, resumed. A registrar invites a guardian **only once BR126's trigger sees a linked child at COMPLETE** — the same anon-key magic link `/platform` already sends, this time to a Person the database can already name. `claim_family_access()` mirrors `claim_club_access()`: it executes the admin's assertion on arrival rather than inferring one, and links `account_person` directly — **no `club_membership` row is ever created**, so nothing here touches the 26 tables WP1–WP3 have yet to narrow. Twelve tables gained an additive `_select_family` policy, scoped by `app_my_family_person_ids()` (decision 11), which is reached through `auth.uid()` and never an argument | **Delivered** |
| **A guardian's household** | `src/app/me/_workspaces/GuardianWorkspace.tsx`, `src/data/household.ts`, `src/web/household-view.ts`, `supabase/migrations/0029_family_reads_documents_and_vouchers.sql` | One card per child — status, what is outstanding, money — and the chosen child's detail beneath, chosen by URL (`&child=`) the way a role is. The cards run **the same rules engine the registrar's queue runs**, so a family and a registrar are never told two different things about one child. That needed 0029: without a family read on `registration_document`, BR2 evaluated against no rows and told a parent every document was attached. An overpaid plan reads as a **credit**, never a negative remaining figure. The card decisions are pure and unit-tested in `src/web/` | **Delivered** |
| **Stopping the email** | `supabase/migrations/0030_communications.sql`, `src/app/unsubscribe/[token]/`, `src/web/unsubscribe.ts` | BR128 built before anything that sends. **Two submit buttons rather than a radio group**: React resets a form after its action, so a person who chose *every email*, hit an error and pressed again would have silently sent *marketing only* — a withdrawal narrowed by a retry. Found by running the page, not by reading it. `app_unsubscribe()` is reached by `anon` — the people it exists for hold no account — and verifies the token in the database exactly as `submit_public_registration` does, so no bypass reason was added for it. A wrong token and an unknown id fail **identically**: confirming an address is on the list is itself a disclosure | **Delivered** |
| **One door for suppression** | `enforce_suppression_through_one_door()` trigger, migration 0030 | An officer may correct an address and may **not** clear a withdrawal with an ordinary update. The flag the two owning functions set is transaction-local, so each clears it before returning — otherwise a caller wrapping an unsubscribe and another write in one transaction would leave the door open behind it | **Delivered** |
| **The pure half of a message** | `src/domain/messaging/` | Templates, recipient resolution and the suppression verdict as pure functions. What a reminder says about a child is asserted in a test rather than discovered in an inbox, and `ComposedMessage` has **no variant without an unsubscribe URL** — a type that permits one will eventually produce one | **Delivered** |
| **Transport, behind an interface** | `src/data/messaging.ts` | Suppression is checked against our own rows **before** the provider is reached (BR129), and every path writes a `message_log` row (BR127). Unconfigured, a send is recorded as `failed` and says so — silence is not a success state, and a reminder nobody received is worse than one the registrar knows failed | **Delivered** |
| **The unsubscribe token** | `deriveUnsubscribeToken`, `src/data/messaging.ts` | `HMAC(server secret, per-row salt)` ([decision 12](../../decisions/12_an_unsubscribe_link_is_derived_not_stored.md)). BR73's hash-only pattern would have killed every older message's link; a plaintext column would have made the table an enumerable list of every address ever contacted. The database stores a salt and a hash, and the secret never enters it | **Delivered** |
| **Telling the family** | `src/data/reminders.ts`, `src/app/registrar/[registrationId]/ReminderPanel.tsx` | The flow the initiative pays for. Outcomes are re-read at send time, not posted from the form — a document provided ten minutes ago must not produce a reminder to provide it — and the result is reported **per guardian**, because "sent to one, suppressed for the other" is the ordinary case in a household | **Delivered** |
| **The notifications the rules asked for** | `src/data/notifications.ts` | BR42's coordinator notification, wired into withdrawal. BR64's fixture change and the claim approval are built and **have no caller**: nothing in the application edits a fixture, and claim approval is database-only (scope 34). Stated in the module rather than left for a reader to discover | **Partial** |
| **Communications tests** | `supabase/tests/33_communications.sql`, `src/domain/messaging/messaging.test.ts`, `src/web/unsubscribe.test.ts` | Ten database scenarios and the pure half. The database one caught the transaction-local flag above, which no amount of reading would have | **Delivered** |
| **Forgetting** | `supabase/migrations/0031_privacy_rights.sql`, `src/domain/privacy/erasure.ts`, `src/app/registrar/privacy/` | BR49's two outcomes and no third ([decision 13](../../decisions/13_erasure_is_all_or_nothing.md)). The verdict is recomputed inside `app_decide_erasure` rather than posted from the screen — a page rendered ten minutes ago may have been looking at a basis recorded since, and erasing a child because a page was stale is not a mistake this system gets to make | **Delivered** |
| **The reasons not to** | `retention_basis`, `src/domain/privacy/types.ts` | One table read by both directions of the machinery. The statutory minimums are per-framework **configuration, not constants**, because nobody here is a lawyer and the code says so | **Delivered** |
| **Retention, proposed** | `app_run_retention_review()`, `src/domain/privacy/retention.ts`, `src/web/privacy-view.ts` | BR40 and BR133 ([decision 14](../../decisions/14_retention_proposes_a_person_disposes.md)). The screen groups disposal proposals apart from everything else and gives only that group an action — a life member listed with the same affordance as a lapsed player is how a club deletes its own history | **Delivered** |
| **What the clock does not reach** | migration 0031, `person_role.role = 'life_member'` | G8, built. A seasonless role enforced in both directions, `deceased_on` as a date rather than a flag, and BR71's contact-review flag for a living life member nobody has confirmed | **Delivered** |
| **Authority at eighteen** | `app_transfer_authority()` | BR67. The one thing in 0031 safe to automate, because it destroys nothing: `is_authority` goes false and `is_contact` is untouched — which is why 0001 made them two flags rather than one | **Delivered** |
| **The club's own copy** | `export_club_data()`, `src/app/registrar/privacy/export/route.ts` | BR68 stops being a claim. Deliberately **not** `stable`, because it writes an audit event: an export is every child's record leaving the building, and who took one is worth asking | **Delivered** |
| **Privacy tests** | `supabase/tests/34_privacy_rights.sql`, `src/domain/privacy/privacy.test.ts`, `src/web/privacy-view.test.ts` | Twelve database scenarios and the pure half. The one that matters most asserts that a **deceased life member is never proposed for disposal** — the row that is otherwise most disposable is the row BR70 exists for | **Delivered** |
| **The competition catalogue** | `supabase/migrations/0032_competition_catalogue.sql`, `src/data/competitions.ts`, `src/app/platform/CatalogueForms.tsx` | Shared reference data, not a club's own ([decision 15](../../decisions/15_the_competition_catalogue_is_shared_reference_data.md)): a per-club minimum would make the same fixture eligible at one club and refused at another because two registrars typed different numbers. The first three tables here with no tenant column, and the only ones every club reads | **Delivered** |
| **BR8, a rule at last** | `src/domain/competition/eligibility.ts`, `src/domain/officiating/conflicts.ts` | Read `conflicts.ts` before 0032 and BR8 carried an apology — *"cannot be completed… no competition record exists"* — so a referee **below** a minimum produced nothing at all. Now a blocker, with the message naming the level required. Four verdicts rather than two, because *cannot judge* is a real answer and collapsing it would be a lie in one direction or the other | **Delivered** |
| **Ranks, confined to one association** | `classification_level.rank`, BR135 | What makes BR8 a comparison rather than a string match. Confined by a trigger, not a convention: a Football Queensland Level 4 and another body's Level 4 are not the same thing, and treating them as such is a guess wearing the clothes of a calculation | **Delivered** |
| **A fixture that can change** | `src/app/registrar/fixtures/EditFixtureForm.tsx`, `updateFixtureAction`, `src/web/fixture-change.ts` | Closes the loose end [scope 36](../../scope/36_the_platform_learns_to_send_and_to_stop.md) left: BR64's notification was built with no caller because nothing edited a fixture. What changed is computed from before and after rather than from the form, so a submit that changed nothing announces nothing — telling people a fixture did not move teaches them to stop reading | **Delivered** |
| **Catalogue tests** | `supabase/tests/35_competition_catalogue.sql` | Seven scenarios, and they are what makes the `check_rls.py` exemption safe rather than merely argued: every club reads the catalogue, **no club writes it**, a platform account holding no membership reads no club's participation, and a minimum cannot cross associations | **Delivered** |
| **Asking whether they also officiate** | `supabase/migrations/0033_officiating_interest.sql`, `src/app/_components/RegistrationForm.tsx`, `src/web/registration-form.ts` | Three questions on both entry points, including the account-free link — a family registering through a link is exactly the family a club needs to ask, and leaving it to the registrar's form would make the answer depend on which door they came in. The declaration never fails the registration: a child registered without their officiating answer is recoverable, the reverse is not | **Delivered** |
| **A claim, not a classification** | `app_declare_interest`, `app_review_interest` | BR136. A parent typing "Level 4" must not reach `referee_classification`, because BR8 now **refuses** designations (0032) — the consequence would not be an untidy record but a child appointed to a match they are not qualified for. Accepting records the level **unsighted**, so BR138 counts it towards nothing until somebody checks the register | **Delivered** |
| **Only a sighted level counts (BR138)** | `src/data/officiating.ts`, `src/domain/officiating/conflicts.ts` | The gap `referee_classification`'s comment had named since 0023 and the code ignored. Harmless while only a coordinator could type a level; unsafe the moment a guardian can. The warning now distinguishes *unchecked* from *none* — one sends a coordinator to the register, the other somewhere else entirely | **Delivered** |
| **Accepting survives BR84** | `app_review_interest`'s exception block | An adult with no verified card cannot hold the referee role, and that trigger is not this function's to argue with — but the *decision* must not be lost with it. The review is recorded, and the screen says a card is what is missing rather than reporting a silent half-success | **Delivered** |
| **The one public surface** | `supabase/migrations/0034_carnivals.sql`, `src/app/events/[eventId]/page.tsx` | P6, built. `createUserClient()` with no token is `anon`, and the additive policy on `published_at` is what makes an unpublished event `notFound()` — the decision lives in the database, so there is no second code path to get wrong, and no service-role key was reached for | **Delivered** |
| **No column to leak** | `carnival_event`, `carnival_entry`, `carnival_fixture` | BR139. The grant is made as narrow as the *schema* can make it rather than as narrow as the policies remember to be. `supabase/tests/37` asserts against `information_schema` that no person column exists, because the whole exception rests on that and a future migration could add one without anybody noticing | **Delivered** |
| **Publication as one act** | `app_publish_event`, `PublishForm` | BR140, explicit, reversible and audited. The screen says what it means in plain words, because "publish" in most products means "save" and here it means *anyone on the internet can read this* | **Delivered** |
| **The ladder** | `src/domain/carnival/ladder.ts` | Pure, and the reason is literal: this is the one thing in the product four hundred strangers read on their phones at a ground, with no registrar to notice it first. A cancelled or abandoned game counts for nothing — counting either invents a fact nobody agreed on — and a dead tie breaks alphabetically so the table does not reorder itself between refreshes | **Delivered** |
| **The calendar feed** | `supabase/migrations/0035_calendar_subscription.sql`, `src/app/calendar/[token]/route.ts` | Decision 4, implemented fifteen months after it was accepted and without re-litigating it — the reasons still held. `app_calendar_feed` takes a token and returns fixed columns (BR141): the holder of a feed URL is unauthenticated by definition, and a feed assembled by filtering something the caller influences is one query-string away from being a different feed | **Delivered** |
| **The token, reused** | `feedUrlFor`, `src/data/calendar.ts` | [Decision 12](../../decisions/12_an_unsubscribe_link_is_derived_not_stored.md)'s construction, not a third one. The unsubscribe link needed *durability* so a year-old email works; a feed URL needs *rotatability* so a lost phone stops reading it. `HMAC(secret, salt)` gives both — a new salt kills the old URL with no revocation list to maintain | **Delivered** |
| **The document** | `src/domain/calendar/ical.ts` | RFC 5545, pure, and pure for a specific reason: the output is read by Google and Apple rather than by a person, so a mistake does not look wrong — it looks like a calendar that silently duplicates every event. **Stable UIDs** are the load-bearing part; folding counts octets rather than characters, because a fold through a UTF-8 sequence produces a line no client can read | **Delivered** |
| **A minor's feed belongs to a guardian** | `enforce_feed_holder()` trigger | BR33. Decision 4 named this as the duty-of-care half of the question rather than the technical half: a calendar feed is a record of where a child will be, and when | **Delivered** |

## Three structural rules for the code

**The rules engine holds no I/O and no framework.** Each business rule is a
pure function from a registration to a result carrying the rule's
identifier and its message. That makes BR1–BR5 unit-testable without a
database, and it makes the rule set the thing a reviewer can diff against
`5_domain-context-and-rules.md` — which is the only way a hundred and twenty-six rules
stay honest against the code.

**The same holds for the screens, and it is enforced rather than asked
for.** What a screen *decides* — which pile a registration belongs in, what
is wrong with a form, which rule is costing the club the most — lives in
`src/web/` as pure functions with no React and no request. The `.tsx` files
render those decisions and little else. `tsconfig.domain.json` typechecks
`src/domain/` and `src/web/` **with no DOM library**, so a `document.` or
`window.` reaching into either fails `npm run typecheck` instead of waiting
for a reviewer to notice. It is checked by proving it fails, not by
assuming it passes.

**Rule identifiers are the business rule numbers.** A `validation_result`
row records `BR55`, not `"legal name mismatch"`. The prose can be reworded;
the identifier is the join between the running system and the architecture,
and it is what makes a failure report readable against the EA documents
years later.
