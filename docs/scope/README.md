# Project Scope Documents

_[← Repository README](../../README.md) · [Enterprise architecture](../ea/README.md)_

One document per delivered (or in-flight) initiative, numbered
chronologically. While the [EA docs](../ea/README.md) describe the
**current** state of the system, each scope document describes one
**change**: what plateau it started from, what it delivered, and what it
deliberately left out.

**ArchiMate viewpoint:** Implementation & Migration (Work Package,
Deliverable, Plateau, Gap).

## The EA-first change process

Every change in requirements follows the same order — the same order the EA
folders are numbered in:

1. **Align the EA first.** Walk the layers top-down and record what the
   change means for each: [1_strategy](../ea/1_strategy/README.md) (does it
   serve an existing goal, or introduce a new driver?) →
   [2_business](../ea/2_business/README.md) (new/changed services,
   processes, rules?) → [3_information](../ea/3_information/README.md)
   (new/changed data objects, flows, storage?) →
   [4_application](../ea/4_application/README.md) (which services,
   components, ports change?) → [5_technology](../ea/5_technology/README.md)
   (any runtime, build, or hosting impact?). Update the affected EA
   documents in the same change.
2. **Document the scope.** Add the next-numbered file to this folder
   describing plateaus, work packages, in/out of scope, and gaps — before
   implementation starts, refined as it proceeds.
3. **Implement.** Only then write the code, keeping the scope document and
   EA docs in sync with what is actually delivered.

Agent guidance for this process lives in `.claude/skills/ea-first-change/`
and `.claude/skills/scope-doc/`; PR descriptions follow
`.github/pull_request_template.md` (see `.claude/skills/pr-description/`)
and must cover the whole branch.

If the project needs a single running index of adopted interpretations that
still need sign-off from a stakeholder who can't be consulted synchronously,
keep it in [open-questions.md](./open-questions.md) — optional, see the
`scope-doc` skill.

If a work package is too large or long-running to implement in one sitting,
shard it into self-contained story files instead of leaving it as one
inline task list — see the `story-sharding` skill.

For a single consequential call smaller than a full initiative — most
often why an AI actor's autonomy level or decision rights were set the way
they were — see [docs/decisions/](../decisions/README.md) (optional) and
the `decision-record` skill.

## Initiatives

| #   | Scope document | Delivered as | Summary |
| --- | --------------- | ------------ | ------- |
| 1   | [1_bootstrap-strategy-and-business-architecture.md](./1_bootstrap-strategy-and-business-architecture.md) | branch `claude/new-session-oizu88` | Converts the Let'sDataTalk discovery document into `docs/ea/1_strategy/` and `docs/ea/2_business/`; no code yet |
| 2   | [2_business-actors-and-open-questions.md](./2_business-actors-and-open-questions.md) | branch `claude/business-actors-pr-76khxv` | Expands business actors from a follow-up discovery document, adds referee-payment and WWCC business rules, corrects Stripe→Square, resolves 11 of 18 open questions; no code yet |
| 3   | [3_competitions-and-calendar-per-season.md](./3_competitions-and-calendar-per-season.md) | branch `claude/multitenant-competitions-calendar-cr62t4` | Adds Competition, Match, Season Competition Entry, Competition Calendar, and the Governing Body / Association actor to the business layer, grounded in the *Australia Competitions per State* reference document; no code yet |
| 4   | [4_voucher-programs-and-committee-approval.md](./4_voucher-programs-and-committee-approval.md) | branch `claude/au-youth-sport-vouchers-jvdr4s` | Generalizes Voucher into multi-program Voucher Program (six AU state schemes); adds Committee-approval (BR21), Finance Admin/Treasurer-only application (BR22), code verification (BR25, Assistant advisory — decision 2), and government reimbursement Voucher Claim (BR23, BR24) rules; records that no state exposes a general claims API at the pilot club's scale and NZ's lack of an equivalent scheme; no code yet |
| 5   | [5_carnival-and-grassroots-event-visibility.md](./5_carnival-and-grassroots-event-visibility.md) | branch `claude/new-session-oizu88` | Adds carnival/grassroots event management (Capability C12, Goal G7): an account-free public view (schedule, next fixture, ladder, results) for coaches, parents, and the general public, and per-event Carnival Conditions configurable only by the responsible Events Coordinator (often the Registrar/Secretary, BR29); adds Principle P6 as a scoped, decision-recorded exception to tenant isolation (P5), the Events Coordinator and General Public/Spectator actors, and BR26–BR29; deferred past the Q4 2026 MVP; no code yet |
| 6   | [6_calendar-distribution-for-referee-appointments.md](./6_calendar-distribution-for-referee-appointments.md) | branch `claude/new-session-oizu88` | Adds Capability C13 (Calendar distribution): referee appointments published as a private, revocable iCalendar feed that Gmail/Outlook/Apple Calendar subscribe to. Decision 4 records why it is a pulled subscription rather than OAuth account access — keeping Principle P2 unamended; adds Calendar Subscription/Calendar Event objects and BR30–BR34 (own-appointments-only, revocable token, no third-party personal data, guardian-issued for minors, platform stays authoritative); no code yet |
| 7   | [7_international-transfer-certificates.md](./7_international-transfer-certificates.md) | branch `claude/business-actors-pr-76khxv` | Grounds the pre-existing `PENDING_EXTERNAL_REGISTRATION` status and Goal G2's "external registration" phrase in Football Australia's ITC process; adds the Football Australia actor, ITC/Minor ITC Application objects, the International transfer clearance process, and BR35–BR38 (ITC required, 30-day provisional-registration rule, FIFA Art. 19 minor exception, Football Australia as sole requester); no code yet |
| 8   | [8_stakeholder-answers-july-2026.md](./8_stakeholder-answers-july-2026.md) | branch `claude/new-session-oizu88` | Records a second stakeholder answering round: resolves 8 open questions and partially answers a 9th. Names SQUADI the system of record (BR39), sets three-year retention (BR40), codifies referee fee determinants (BR41), and requires a reason on decline/withdrawal (BR42, Appointment Response object); records Queensland Play On! as the approved voucher program. Surfaces a blocking conflict — the confirmed success metric (SQUADI sync) depends on an integration deferred past the MVP with no API (question #27); no code yet |
| 9   | [9_staged-registration-and-governing-body-tier.md](./9_staged-registration-and-governing-body-tier.md) | branch `claude/new-session-oizu88` | Resolves the blocking MVP scope conflict (#28) by staging rather than trade-off: registration proves itself club-native first (pre-MVP, no integration), then SQUADI, then Football Australia — only stage 1 unconditional. Records serving a governing body (e.g. Football Queensland) as a Course of Action under consideration, with its Principle P5 collision named and deliberately left unmodeled (#31); adds Playing Format and Competition Regulation objects and extends C11; no code yet |
| 10  | [10_registration-baseline-and-the-squadi-gate.md](./10_registration-baseline-and-the-squadi-gate.md) | branch `claude/new-session-oizu88` | Records the measured registration baseline — **weeks**, caused by SQUADI's usability — and its consequence: under Football Queensland policy an unregistered player cannot take the field (BR43), so latency costs playing time. Restates stage 1 of the registration ladder from "club-native" to **SQUADI-ready** (first-time-right submission) so it attacks the actual bottleneck, and names the portion of the delay no data-entry improvement can remove; raises #32 to decompose the weeks before any target is set; no code yet |
| 11  | [11_majestri-and-the-reconciliation-gap.md](./11_majestri-and-the-reconciliation-gap.md) | branch `claude/new-session-oizu88` | Records **Majestri** as the incumbent club management system most clubs already run, and the counted 2026 reconciliation gap (799 players; 78 missing from PlayFootball, 40 from SQUADI, as at 15 May 2026). Adds Capability C14 (External registration reconciliation), its service, process, four objects, and BR44–BR47 — covering matching without a shared key (SQUADI dropped FA ID in March 2025), extracts that carry their own blind spots, dated/stale results, and gaps that are eligibility exceptions under BR43. Raises the Majestri positioning question (#33); no code yet |
| 12  | [12_privacy-consent-and-safeguarding.md](./12_privacy-consent-and-safeguarding.md) | branch `claude/new-session-oizu88` | Closes a safeguarding gap: BR19 verified a WWCC at onboarding and BR10 blocked *new* designations, but nothing removed someone already on a future match sheet when their clearance lapsed. Adds Principle **P7**, Capability **C15**, and BR48–BR52 — recorded revocable guardian consent, a right-to-erasure protocol with recorded lawful-basis exceptions, WWCC re-verification, and **automatic withdrawal from all future assignments on expiry** with coordinator notification. Amended August 2026: BR51 restated around **register notification to the linked club** rather than platform polling (no programmatic access needed), plus **BR54** — test a clearance against the *end of the season*, not against today, since a visual check cannot see a mid-term cancellation. Records that the APPs and NZ Privacy Act contain no general right to erasure, so GDPR is adopted as a design standard rather than a binding requirement (#36); no code yet |
| 13  | [13_squadi-access-refusal-and-the-terms-of-use-constraint.md](./13_squadi-access-refusal-and-the-terms-of-use-constraint.md) | branch `claude/new-session-oizu88` | Records Football Queensland's 31 July 2026 reply to the pilot club's written API request — access is restricted to Football Australia, FQ, and **approved system partners**, and is not issued to affiliated clubs. Restates stage 2 of the registration ladder as depending on **partner status** rather than a technical request. Surfaces the larger finding: the terms of use "do not permit club accounts or Squadi data to be connected or integrated with unauthorised third-party systems", which reaches the **CSV fallback** C14 depends on, not only the API — and which Principle P2 (read-only *write-back*) does not answer. Adds **BR53** (authorisation basis per source; unconfirmed means not permitted) and questions #39–#43. Also records the club's August 2026 reply, which withdraws the API request and asks instead whether Squadi already supports **club-side bulk/assisted submission** (#42) — and notes that stage 2 needs a scoped **P2** exception whichever route wins, since a permitted write is still a write (#43); confirms Football Queensland as the pilot club's governing body (#19, partially); no code yet |
| 14  | [14_submission-pack-and-the-inverted-direction.md](./14_submission-pack-and-the-inverted-direction.md) | branch `claude/new-session-oizu88` | Football Queensland never replied, so the integration direction is **inverted**: Let'sDataTalk becomes the source of truth for the club's own registration data and emits an immutable, versioned **Registration Submission Pack** the federation imports into Squadi itself. The platform produces a file and never touches Squadi, which **side-steps** (not resolves) #39's terms-of-use question, BR53, and #43's P2 exception. Adds Capability **C16**, rules **BR58–BR60**, two objects, and a new stage 1.5 on the registration ladder. **Restates BR39**: authority splits by question — the club is the source of truth for what it submitted, SQUADI stays authoritative for eligibility, and **sending is not registering** (BR60). Ships operating instructions as an annex; raises #46, since FQ has not agreed to import anything; no code yet |
| 15  | [15_role-based-mobile-experience.md](./15_role-based-mobile-experience.md) | branch `claude/new-session-oizu88` | Adds Capability **C17**, the platform's first direct surface for a `Person`: one app showing the active role's context with an explicit switch, which is where Principle **P1** stops being an internal property and becomes something a user experiences. Adds **BR61–BR65** — one active role context at a time so the convenience is not a leak; availability responses extended from referees to players with the reason visible to the responsible coach only; guardian owns a minor's account and responses (mirroring BR33); fixture changes notified to participants, closing the lag BR50's blast-radius note left open; and each person sees **their own** eligibility gap, turning the reconciliation backlog into self-service. Records the go-to-market stance: arrive when club pain is loudest, but never market against the federation, since that contradicts the partner, API, and customer conversations the project still needs. Raises #47 — offline behaviour, now blocking the client architecture; no code yet |
| 16  | [16_stakeholder-answers-august-2026.md](./16_stakeholder-answers-august-2026.md) | branch `claude/new-session-oizu88` | A third answering round worked through the pending ledger: **eleven questions resolved**, four partially answered or reframed, one raised — the largest single reduction the project has had. Confirms two support tiers (36-hour standard, paid Platinum), a A$12,000 first-year contract, **Queensland-first** market entry, carnivals as **MiniRoos-only at 80–90 teams**, the ITC guide as current, GDPR as a voluntary design standard, that SQUADI's FA ID has **not** returned, and that **Majestri supports bulk export** — which makes decision 5's displacement strategy executable. **#41 reframes the FQ problem**: the platform is founder-owned and can take a club, or FQ itself, as partner or investor, turning partner status from a permission requested into a relationship offered. **Restates BR40** to status-based retention (≥10 years active, ≤2 years inactive) — which makes #30 more urgent, since the new two-year *ceiling* may fall below statutory minimums and would gate a disposal job. Extends BR41 (each Committee authors its own fee schedule) and adds **BR67**, the recorded transfer of control at 18. Raises #48 on the renewal model; no code yet |
| 17  | [17_mvp-registration-slice.md](./17_mvp-registration-slice.md) | branch `claude/new-session-oizu88` | **The first initiative that will produce code.** Starts the three unstarted EA layers and defines the thin vertical slice the build begins with: one-shot registration for the pilot club. Records the stack — **Next.js + Supabase + Vercel**, Sydney — chosen on one property, that Supabase enforces **P5 tenant isolation in the database** rather than in application code, so a forgotten filter returns nothing instead of everything; a table shipped without an RLS policy fails CI. Settles the **P1 vs P5 collision** by scoping `person` to the tenant: one identity per club, with cross-club identity left where it already belongs, in BR44's matching and C14. Models consent as **rows per purpose rather than booleans**, persists validation results so #32's decomposition becomes a query, and ships **retention tracking without disposal** — nothing deletes automatically until #30 is answered. Notably adds **no new business rule**: everything the slice implements already existed, which is the EA-first process working. Scope: C1, C2, C6, C10, C15, C16 — no finance, referees, mobile, or C14 |
| 18  | [18_life-members.md](./18_life-members.md) | EA alignment only | Adds Goal **G8** and Capability **C18** (Life member register & recognition), raised by stakeholder request. Models Life Member as an **indefinite `Person Role`** — no season, granted once, held until revoked or death (**BR69**) — a deliberate exception to every other role's season-scoped pattern. Adds **BR70**: a deceased life member's record is retained **indefinitely**, overriding BR40's participation-based retention window, and is never contacted. Adds **BR71**: a living member's contact details are flagged for review if not reconfirmed within two years, mirroring BR46's staleness pattern. Deferred past the Q4 2026 MVP the same way carnivals (C12) were — aligned now, built later; no code yet |
| 19  | [19_tokenised-family-link.md](./19_tokenised-family-link.md) | branch `claude/new-session-oizu88` | Closes scope 17's last gap: the family registration form required a club login, making it registrar-assisted intake rather than something a parent completes at home. Adds **BR72** (registration by an unguessable, revocable link scoped to one club and season — write-only, no account) and **BR73** (the token stored as a *hash*, shown once, expiring, uses counted). Needed its own pass because the security model had **nowhere for an unauthenticated writer**: every policy keys off `auth.uid()`. Resolved by one `security definer` function whose tenant is **not an argument** — it comes from the invitation the token resolves to — rather than a service-role write, which would have made P5 depend on application code being correct. **No P5 exception:** the link writes to one named club and reads nothing. Recorded as [decision 6](../decisions/6_public-registration-through-a-scoped-function.md); proved across 18 behavioural scenarios |
| 20  | [20_identity-membership-and-registration-requirements.md](./20_identity-membership-and-registration-requirements.md) | branch `claude/identity-and-registration` | Implements **C1** and completes **C2**'s requirements half — and in doing so finds a defect: **BR2 has passed on every registration ever made, and could not have failed.** The rule compares a registration's required documents against its provided ones, and nothing in the system ever created a `registration_document` row, so empty minus empty reported *pass* while the queue showed the safeguarding check as done. BR3 was vacuous for the same reason. Fixed where registrations are created, not in the screens that read them: **`season`** gains `required_document_types` and `registration_fee_cents`, both stamped onto a registration as it is made. The same edit closes a second gap — `person_role` had a table, policies and a check constraint since 0001 and **no code at all**, leaving **P1** true in the schema and unrepresented in running software; a public submission now writes the child's `player` and the guardian's `guardian` role as it creates them. Adds the **people directory** (one row per human, roles granted and revoked per season), the season requirements screen, and document/fee handling on the registration detail. Existing registrations are deliberately **not** backfilled with a checklist — a family cannot be held to a requirement they were never told about — so applying it is a registrar's audited act; roles *are* backfilled, because they are derived rather than invented. **Adds no business rule**: what was missing was never a requirement, it was the data the requirements were about. Proved across 15 further behavioural scenarios (53 in total), verified to fail |
| 21  | [21_payment-plans-and-the-registration-status.md](./21_payment-plans-and-the-registration-status.md) | branch `claude/identity-and-registration` | Checks the **player registration** and **payment plan** processes against what the code does. Registration was built but its **status never moved**: `PENDING_DOCUMENTS` and `PENDING_PAYMENT` have existed since 0001 and had never once been written, so the two statuses that exist to say *why* a registration is waiting were decoration — a registration sat at `DRAFT` until a pack carried it away. Now derived from the rule outcomes, and deliberately unable to produce `COMPLETE` or `PENDING_EXTERNAL_REGISTRATION`, which stay the federation's and the pack's (BR43, BR60). The payment plan process was a diagram and nothing else; this is the **first piece of C3 to exist** — plans, instalments, and recorded receipts, with no payment provider, because the club's own books must be right whether or not an integration arrives. **Restates BR3**, the one change to documented behaviour: what blocks a registration is **arrears**, not balance. Under the old rule a family three weeks into a five-month plan, having paid everything asked of them, would have been held out of the season — the club could offer a plan and the child still could not play. Adds **BR74–BR78**: instalments sum to exactly the total with the odd cents on the *first* one, at most one live plan, the plan finishes inside the season, receipts are **append-only** with corrections as reversing entries, and only Finance Admin or Treasurer may move money (BR22's separation). BR74 and BR77 are enforced by the **database** — a deferred constraint trigger and the deliberate absence of an update policy — because this application is not the last thing that will ever write these tables. Proved across 15 further behavioural scenarios (68 in total), verified to fail, plus a simulated season that reconciled to the cent |
| 22  | [22_no-pay-no-play.md](./22_no-pay-no-play.md) | branch `claude/identity-and-registration` | Records a club policy that was never written down and **reverses a decision made one initiative ago**. Scope 21 restated BR3 from balance to *arrears*, reasoning that a family honouring an agreed plan should not be held out of the season; the reasoning was sound and the premise was wrong — the pilot club does not distinguish owing money from owing money on a schedule. **BR3 goes back to blocking on any outstanding amount**, with scope 21's plan-awareness kept but demoted from deciding the verdict to writing the message. Adds **BR79 — no pay, no play**, and implementing it found the case neither BR3 nor the status can reach: once a registration is `COMPLETE` its status is deliberately frozen, because the club must never appear to revoke an eligibility the federation conferred (BR60), so a player confirmed in round 1 and charged in round 5 — or whose payment is reversed under BR77 — **keeps a COMPLETE status while owing money and looks finished on every screen**, and a coach picking from the Registered list would field them. Eligibility therefore becomes its own question, asked fresh from the status *and* the balance every time and never stored. Surfaced on the queue as a *Registered, but cannot play* section with the total owed, plus a money pill on every card whether or not it blocks; visible to Registrar and Treasurer alike, since the queue is reachable by any club member. **No information-layer change** — storing eligibility would create a third fact that can disagree with the two it came from. Raises #50 (hardship override, deliberately not invented) and #51 (whether BR79 should bench a child while a government voucher claim is pending) |
| 23  | [23_vouchers-and-sibling-registration.md](./23_vouchers-and-sibling-registration.md) | branch `claude/identity-and-registration` | Both halves come from **MiniRoos**: most of those families hold a government voucher, and most have more than one child. Fixes a quiet bug first — `submit_public_registration` inserted the guardian **unconditionally**, so a parent registering a second child became a *second Person* and a third child a third. Since a guardian carries the placeholder date of birth, the copies share a name and a DOB and BR5 flags them, which means **the club's reward for a family having two children was a duplicate-resolution task the platform generated itself**. **BR80** matches the guardian on normalised email before inserting — not the silent merge BR5 forbids, since nothing existing is combined, a second copy is simply never created — and the public form gains a *Register another child* step that carries the parent forward and resets the child. Then vouchers: attach one with its PDF, and a club officer decides. **BR81** is load-bearing — *attaching changes nothing*. An `ATTACHED` voucher reduces no balance, so BR3 keeps failing and BR79 keeps the player off the field, because a PDF is a **claim** that a discount applies, not the discount; one that turns out to be expired or already spent would otherwise have let a child play on money the club never receives. Verification applies the value as an ordinary **payment**, so relief shares one ledger with every other receipt and a rejection reverses it under BR77. A registrar may attach (collecting the document is registration work) but only an admin or treasurer may verify (BR78). First use of **Supabase Storage** — a private bucket, paths beginning with `club_id`, policies checking that segment — and the **first unproved thing in the slice**, since the local test Postgres has no `storage` schema; `check_rls.py` was taught to recognise Supabase-owned tables rather than weakened. Proved across 17 further behavioural scenarios (85 in total), verified to fail. Resolves #51; raises #52 (should the family upload the PDF themselves) |
| 24  | [24_one-creation-path-and-duplicate-resolution.md](./24_one-creation-path-and-duplicate-resolution.md) | branch `claude/identity-and-registration` | Found by reading the pilot club's live data, not from a report. Every rule added since scope 20 went into `submit_public_registration`, while the **registrar-assisted form built its own registration out of six inserts** — so the club had **two implementations of one process and only one was correct**. Registrations made through the club's own screen came out with no roles, no checklist, no fee and a freshly duplicated parent each time; production held 4 registrations, 2 roles, and **one parent recorded four times** under four spellings sharing one email. The fix is not to copy the rules across — that is how it happened — but to delete the second path: `app_create_registration` holds the process once, with a definer wrapper for the token-bearing family link and an invoker wrapper where RLS decides for the club screen. The registrar action drops 200 → 113 lines. `tests/16` creates a registration **each way and demands the same shape**, which is the durable part: restoring the divergence makes it name all four defects. Then resolution — BR5 could flag duplicates but never clear one, and flagged them *per registration*, so guardians (the records that actually duplicate) appeared on no screen at all, which is how they reached four. **BR5 restated on the club's rule: email settles it in both directions** — shared email *and date of birth* is one human whatever the names say, while the same name with *different* emails is two humans and is no longer raised. The date of birth still has to match, because siblings share a family address and matching on email alone would declare two children to be one. **BR82** adds a human-chosen survivor: `merge_person` repoints every registration, role, guardianship and consent in one transaction and leaves a tombstone, never a delete. Existing duplicates are deliberately **not** auto-merged. Proved across 12 further scenarios (97 in total), verified to fail. Raises #53 |
| 25  | [25_teams-and-team-officials.md](./25_teams-and-team-officials.md) | branch `claude/identity-and-registration` | The club is organising several teams and had nowhere to put them: **Team** was named in passing since scope 3 and modelled nowhere, so nothing could answer *which players are in which team*. Adding teams is the small half. Adding **Team Officials** is not — a Team Official is a child-related role, so **BR19** applies (Queensland's rule is literally *no card, no start*) and **BR54** decides what current means: not *is the card valid today* but *does it cover the season this team plays*. A coach whose Blue Card expires in round 12 has not passed, they have failed later, and the club finds out on a Thursday with a fixture on Saturday. Nothing about clearances existed in the schema, so P7 and C15 — written in scope 12 precisely to close this gap — would have been re-opened by building officials without them. Adds **Team**, **Team Membership** and **Clearance** objects, and **BR83** as a **database trigger** rather than a screen check: this is the one rule in the project whose failure mode is a child standing in front of an uncleared adult. Three deliberate properties — *verified* is separate from the card number (holding a number is not a check), the trigger fires on **update** as well as insert (promoting a player row to a coach row is the obvious way round), and it measures against `season.ends_on`. Also answers the club's ask for a button showing a lost registration link: **there is none and there cannot be**, since only the token's hash is stored (BR73) — so *Reissue* revokes the old and shows a replacement once, and the screen says why. Proved across 13 further scenarios (110 in total), verified to fail. Raises #54: BR83 makes *appointment* safe and leaves *continuation* unguarded, since BR50's automatic withdrawal needs a scheduler the platform does not have |
| 26  | [26_match-officials-and-club-governance.md](./26_match-officials-and-club-governance.md) | branch `claude/identity-and-registration` | Extends the clearance rule from team officials to **match officials** (**BR84**) — and carries a correction the ask did not contain. Implemented literally it would have been **wrong in law**: Queensland exempts volunteers under 18 from the Blue Card, and Football Queensland's referee pathway **starts at MiniRefs**, twelve-year-olds officiating under-7s. The existing suite caught it before the rule was written — `tests/13` grants a referee role to a child, and was right to. BR84 exempts under-18s, measured at season end so someone turning 18 mid-season counts as the adult they will be; scope 25's team trigger had the same latent error and is corrected. The exemption is asserted as hard as the requirement, because an over-strict safeguarding rule fails *quietly* — the club works around it, and the workaround is where the real risk goes. Adding the trigger then **found a real bug**: `merge_person` moved a person's roles and left their **clearance** behind, so merging a coach stripped the card and the new trigger rejected the survivor's own role. Scope 24 predicted exactly this and could not prevent it (*"the next table added needs to be considered against merge_person, and there is nothing that forces that consideration"*); two tables later, nothing did. Fixed by repointing `clearance` **first**, plus `team_member` and `committee_position`. Then governance: new capability **C19**, with **BR85** (a position is held for exactly one Committee Term, AGM to AGM, lapsing rather than being revoked) and **BR86** (an overdue term still governs but its mandate is unrenewed — and the end date is **stated by the club, not computed**, because a club that meets three months late has a real authority question that arithmetic would answer wrongly). This gives BR21's *"the Committee approved it"* something to point at. Proved across 17 further scenarios (127 in total); removing the exemption and the clearance move breaks **three** suites. Raises #55 |
| 27  | [27_committee-adults-and-card-scans.md](./27_committee-adults-and-card-scans.md) | branch `claude/committee-adults-and-card-scans` | The committee screen offered **everybody**, including the nine-year-olds the club had just registered. **BR87**: a Committee Position may only be held by an adult, measured at the **term's start** — a parent may serve, a Life Member may serve, and what excludes a MiniRoos player is that they are a child, never that they play. A trigger rather than only a filtered dropdown, because the dropdown is a courtesy and the constraint is what makes it true. The same list offered a child on the screen recording a **Working with Children Check** — a card they cannot hold and are exempt from needing (BR84) — so that list is adults only too. The club's answer settles **#55**: committee and subcommittee members *do* need a check, codified as **BR88** with one deliberate difference — it is **surfaced, not enforced**. The same stakeholder had already instructed that the committee be identified *first*, so blocking on a card would make the two instructions contradict; a coach stays blocked outright because the role puts them in front of children on a fixture date, while a treasurer is seated with the gap shown. An unrecorded committee is worse for safeguarding than a recorded one with a visible gap. Clearances now carry a **scan** in a private bucket narrower than the vouchers one, since a card is a government identity document — and the scan is evidence, never verification: uploading an image does not tick the verified box, because BR19 wants the number checked against the state's portal. Finally, the club noticed the clearance form said nothing on success — and **no form in the application ever had**. Every action returned a message on failure and *nothing at all* when it worked, which is indistinguishable from a dead button and teaches people to press twice. Fixed as a class: one `FormResult` shape, one `FormNotice`, and a clearance confirmation that distinguishes *recorded* from *recorded and verified*. Proved across 9 further scenarios (136 in total), verified to fail |
| 28  | [28_onboarding-a-club-and-its-history.md](./28_onboarding-a-club-and-its-history.md) | branch `claude/tenant-provisioning-and-historical-data` — **documentation only, nothing built** | Five decisions about how a club becomes a customer and what happens in the hour after. **Tenancy is authorised by the platform owner alone**, on a contract signed directly — delivered by a single-use owner-issued link that generalises decision 6 one level up, recorded as **[decision 7](../decisions/7_tenant-provisioning-by-owner-issued-invitation.md)** and **BR89**. Self-service is ruled out by the *commercial* model before the security one. **Central super-administration stays unbuilt** and needs its own decision record, because a cross-tenant reader is a P5 exception — and provisioning does not require one. Flags that the nominated super-admin address no longer exists: it was renamed to the club admin's on 23 August, leaving one account that is simultaneously platform owner, club admin and registrar, which is exactly the conflation to design out. The website **explains, qualifies and captures**, writing outside the tenant world entirely. The substantial part is **a club's history**: clubs sign to get their previous years back, so **C9 is restated** from a one-off read-only extraction against the pilot club's systems to a repeatable **club-supplied export** — which sidesteps BR53, since a club exporting its own data has an unambiguous basis under BR68. Adds **BR90 — imported history is history**: never in the queue, never in a submission pack, never chased, never eligible. Without it a 700-player three-season import floods the queue and the first pack tries to register the class of 2023 with the federation, looking like a successful migration until it goes out. Records the traps before anyone hits them — duplicate resolution at migration scale versus BR5's never-merge-silently, and that importing five years of children's data is a **privacy decision, not a data-engineering one**. Demo tenant agreed: one permanently-seeded club, obviously fictional data, a real tenant protected by P5 like any other. Raises #56 and #57 |
| 29  | [29_actors-access-and-permissions.md](./29_actors-access-and-permissions.md) | branch `claude/governance-actors-and-permissions` — **audit complete, not implemented** | Three questions about club governance: how an actor signs in, what each may do, and what each may see. Answered from `pg_policies` rather than from the documentation. Findings: access can only be granted by hand-typed SQL though the policy already permits a screen; **`committee`, `coach` and `viewer` have identical permissions**; a role restricts writing and barely restricts reading, so a coach sees every family's balance and consents club-wide; and **nothing links a login to a Person**, so the system cannot recognise the President when she signs in. Raises [#58–#60](./open-questions.md) |
| 30  | [30_the-player-record-and-what-a-statistic-costs.md](./30_the-player-record-and-what-a-statistic-costs.md) | branch `claude/player-record-and-statistics` | A player card — headshot, physique, position, appearances, minutes, goals, assists — which turned out to need a **match model** first: twenty-eight tables and not one knew a game had happened. Builds fixtures and appearances carrying **counts rather than events**, narrows physique to the roles that pick teams (BR99), and records who entered each statistic because there is nothing to check it against (BR101). The advanced tier — xG, shots, big chances — is designed and **not built**, with each metric's real cost written down, including that an adult xG model applied to under-9s produces confident numbers that describe nothing (BR104). Raises [#62–#64](./open-questions.md) |
| 31  | [31_the-interface-and-the-southern-ocean-palette.md](./31_the-interface-and-the-southern-ocean-palette.md) | branch `diseno-ux` | Thirty screens had been built and none had been **designed**: one stylesheet grown page by page, a framework's default blue, and four classes the markup used that no rule ever defined. Replaces it with a token system — colour ramps, space, elevation, type, motion, a dark theme derived rather than hand-listed — under the **green and gold** that means football in Australia and sits under New Zealand's white without competing with it. Two rules keep it usable: gold never carries text (1.6:1 on white), and no state is signalled by hue alone, because the thing a pill marks is whether a child may take the field. Adds a mark, a sticky masthead, a skip link, a hero and a footer; **adds no framework, no font and no dependency**. Per-club branding is explicitly *not* closed — raised as [#65](./open-questions.md) |
